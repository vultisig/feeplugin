apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: plugin-fee
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    cert-manager.io/cluster-issuer: "letsencrypt"
spec:
  ingressClassName: traefik
  tls:
    - secretName: plugin-fee-tls
      hosts:
        - plugin-fee-server.lb.services.1conf.com
  rules:
    - host: plugin-fee-server.lb.services.1conf.com
      http:
        paths:
          - path: /
            pathType: ImplementationSpecific
            backend:
              service:
                name: server
                port:
                  number: 80
